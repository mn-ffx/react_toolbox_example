import React from 'react'
import {theme} from './style.css'
import Button from 'react-toolbox/lib/button/Button'

const ImageButtonBig = (props) => <Button {...props} theme={theme} />

export default ImageButtonBig
