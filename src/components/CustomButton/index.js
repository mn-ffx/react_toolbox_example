import React from 'react'
import {theme} from './style.css'
import Button from 'react-toolbox/lib/button/Button'

const CustomButton = (props) => <Button {...props} theme={theme} />

export default CustomButton
