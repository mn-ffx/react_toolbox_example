import React from 'react'
import {theme} from './style.css'
import Button from 'react-toolbox/lib/button/Button'

const ImageButtonSmall = (props) => <Button {...props} theme={theme} />

export default ImageButtonSmall
