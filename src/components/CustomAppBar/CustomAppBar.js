import React from 'react'
import {theme} from './style.css'
import AppBar from 'react-toolbox/lib/app_bar/AppBar'

const CustomAppBar = (props) => <AppBar {...props} theme={theme} />

export default CustomAppBar
